# 学习方法论

> 这份文档是你整个 6 个月学习路线的**执行纪律**。
> 打开 `daily/day-NN.md` 前先看这里，学习结束时对照这里反思。

---

## 一句话原则

**用 AI 加速你手上的键盘，不要用 AI 加速你脑子里的思考。**

---

## 学习的 4 个层次

```
Layer 1  执行层     写代码 / 跑命令 / 调 API          AI 加速 70-80%
Layer 2  理解层     懂概念 / 懂原理 / 懂取舍          AI 辅助，不替代
Layer 3  判断层     选型 / 预判风险 / 识别反模式      AI 几乎无法替代
Layer 4  肌肉记忆   不查文档能手写出来                AI 替代 = 自毁
```

**越靠近 Layer 4 越不能用 AI；越靠近 Layer 1 越该用 AI。**

---

## Layer 1 · 执行层 ✅ 大胆用 AI

### 该用的场景
- "写一个 Drizzle schema 模板，4 张表含外键"
- "这个 TypeScript 类型错误怎么改"
- "debug 这个 CORS 错误"
- "生成一个 seed 脚本"
- "补全这段 Server Action 的 envelope 包装"

### 唯一纪律
**读 AI 生成的每一行代码**，理解为什么这样写。这个"读"的动作本身是 Layer 2 练习。

### 为什么可以大胆用
这些是**已知解法**的事。你花 30 分钟查文档写出来，和 AI 10 秒写出来，**学到的东西一样多**。省下的时间投入 Layer 2-4 收益更大。

---

## Layer 2 · 理解层 ⚠️ AI 辅助，不替代

### 该做的
1. 先自己读**官方文档 / 一手博客 / 作者视频**，建立第一次心智模型
2. 自己写 3 条笔记（哪怕是错的）
3. **然后**让 AI 验证或挑战你的理解
4. 如果 AI 的答案和你的理解冲突，**回去重读 docs 验证** —— 不要盲信 AI

### 不该做的
- ❌ "帮我讲讲 X 是什么"（诱导被动吸收，一周后忘光）
- ❌ 让 AI 替代读官方文档（docs 是作者亲说，AI 是模型推测）
- ❌ 让 AI 帮你摘要长文章（摘要会失真，而且你没经过理解过程）

### 好问法 vs 坏问法

**坏**：
```
Claude 给我讲讲 Drizzle relations
```

**好**：
```
我理解 Drizzle 的 with 在底层会产生一次 JOIN，对吗？
relations() 和 foreignKey 为什么要写两遍？
prepared statement 在 serverless 环境下到底有没有用？
```

区别：**坏的问法让 AI 主导**，好的问法让**你主导**，AI 只是校验。

---

## Layer 3 · 判断层 ⚠️ AI 是挑战者，不是决策者

### AI 给不了的东西
- 真实踩过的坑（AI 没踩过）
- 对你当前项目约束的理解（AI 不知道）
- 对未来变化的直觉（AI 无此能力）

### 典型反例
❌ "我该用 Drizzle 还是 Prisma？" → AI 会说"两者都很好看团队偏好" —— **无用答案**

✅ 该这么用：
```
我考虑用 Drizzle 而不是 Prisma，理由是：
1. 未来想上 edge runtime
2. 想要更贴近 SQL 的控制力
3. 团队只有我一个人

请挑出这个决定里最危险的盲点。
```

这样用 AI 是**质量检查员**。

### 判断类问题的流程
1. **自己想**，列出 3 个选项 + 3 个判据
2. **再问 AI 挑战**，不是让 AI 提新方案
3. 采不采纳 AI 的反馈由你决定

---

## Layer 4 · 肌肉记忆 🚫 AI 替代 = 自毁

### 这些必须不靠 AI 能手写

作为 Agent 工程师，以下能力**必须内化为肌肉**：

**协议层**
1. Anthropic Messages API 的 fetch 调用（含 SSE parser 的 `\n\n` 边界处理）
2. Tool use 完整报文流：`user → assistant(tool_use) → user(tool_result) → assistant`
3. Streaming event 类型：`message_start / content_block_delta / message_stop`

**Agent 核心**
4. ReAct loop 的 while 循环骨架（含 `max_iterations`、停止条件、错误恢复）
5. tool 定义的 zod schema + execute 函数签名
6. Prompt injection 的 5 种攻击面 + 分层防御

**生产工程**
7. 一个正确的 Server Action（含 auth + 错误 envelope）
8. 一个带 `step.run` 的 Inngest function（含 retry + onFailure）
9. Stripe subscription 的完整状态机图
10. SSE server 的 `ReadableStream` + `request.signal` 监听模板

**这 10 项是面试题 / 上线救命 / 博客选题的本钱。**

### 为什么不能用 AI

- 面试时面试官让你白板手写 tool use loop，你没时间问 AI
- 生产 bug 凌晨 2 点炸了，你面对的是一段复杂代码，**肌肉记忆决定你能不能救命**
- 真没内化时，你会产生"我懂了"的错觉 —— 这是**最危险的状态**

### 训练方法

- **每周至少 1 次"合上所有工具从零手写"**（daily 里的周复盘日已内置）
- **闭卷口述自检**（每个 Day 末尾的 "今天结束能回答" 严格闭卷）
- **M2 Week 5-6** 的手写 SSE parser 和 ReAct loop —— **这两周彻底不碰 AI 代码补全**。这是 6 个月里最重要的 14 天

---

## Daily 文件的 AI 使用对照表

| `daily/day-NN.md` 段落 | AI 使用 |
|---|---|
| **今天学什么** | ❌ 不用 AI。自己读完产生理解后，可以问 AI "我这么想对吗" |
| **核心概念** | ❌ 不用 AI 第一轮讲解。读 docs 建立第一版理解 |
| **参考资源** | ❌ 别让 AI 替你读。资料质量是作者本人保证的 |
| **动手练习（模板代码）** | ✅ 大胆用 AI。写骨架 / debug 错误 / 补 TypeScript 类型 |
| **动手练习（业务逻辑）** | ⚠️ 自己写第一版，AI 帮 review |
| **卡点思考** | ❌ **绝对不要**问 AI 答案。这是 Layer 2-3 的精华 |
| **今天结束能回答** | ❌ 不能用 AI。作弊就白学了 |
| **晚上 journal** | ❌ 不用 AI。这是你自己的学习感受记录 |
| **周复盘 / 口述自检** | ❌ 完全不用 AI。学习产出证明 |

---

## 可以问 AI 的 5 种问题

1. **执行型**："写一个 [具体、局部] 的代码片段"
2. **验证型**："我这么理解 X 对吗？"
3. **挑战型**："我的方案有什么盲点？"
4. **Debug 型**："这个错误信息什么意思？"
5. **对比型**："Y 和 Z 各自的取舍是什么？"（快速对比，但最终结论自己下）

## 不能问 AI 的 4 种问题

1. ❌ "帮我讲讲 X"（太宽泛，诱导被动吸收）
2. ❌ "Day N 我该怎么做"（roadmap 和 daily 已经写清楚了）
3. ❌ "给我 [自检题] 的答案"（作弊）
4. ❌ "帮我总结 [长文]"（摘要失真，你没经过理解过程）

---

## 具体场景对比

**场景**：Day 11 做 Stripe webhook 处理 4 种 event。

### 方式 A · 纯传统（3.5h）✓ 扎实但慢
- 读 Stripe docs 1.5h
- 手写 webhook handler 1.5h
- Stripe CLI 测试 0.5h

### 方式 B · AI 盲用（35 min）✗ 快但空
- Claude 写完整 handler 5 min
- 抄代码跑通 30 min
- **代价**：3 个月后让你写新场景的 webhook，写不出来

### 方式 C · AI 正确使用（2.5h）✓ 推荐⭐
- 读 Stripe docs 45 min（精读概念，跳过例子代码）
- 合上文档，自己画 subscription 状态机 20 min
- 让 AI 检查状态机：**"我画的这个状态机有哪里错或缺"**
- 让 AI 写模板代码骨架 15 min
- 自己填业务逻辑（验签、去重、DB 写入）45 min
- 故意触发错误测试 30 min

**节省 1h，理解深度 ≈ 纯传统方式。**

---

## 每日执行公式（打印贴显示器）

```
[读 docs]   ← 30-60 min 自己读，不用 AI
[想一想]    ← 合上文档，自己在纸上画 / 写 / 说
[AI 验证]   ← "我这么理解对吗" "有什么盲点"
[AI 写骨架] ← 模板代码让 AI 快速生成
[自己填肉]  ← 业务逻辑自己写，读懂每一行
[闭卷自检]  ← 今天结束能回答，不查资料
[记 journal]← 3 行：aha / 疑问 / 想深挖
```

---

## 反直觉的建议

**别因为"AI 能做"就跳过这一步。**

Day 22 手写 SSE parser，Claude 1 分钟能写完。但**你自己花 45 分钟写一遍**价值比用 AI 生成高 10 倍：

- 你感受了 `\n\n` 边界问题
- 你踩了 `TextDecoder` 的 `stream: true` 坑
- 你懂了 `ReadableStream.start()` 什么时候被调用
- 这些**感受**在 M2 Week 5 写 Anthropic SSE parser 时会让你秒懂

AI 省的时间要投入 Layer 2-4，**不是用来做更多 Layer 1**。否则你只是"跑得更快地跑向无知"。

---

## 自检：这周我是不是在用 AI 偷懒

每周日复盘时问自己：

- [ ] 本周有没有 "AI 写完我抄过来" 的情况？
- [ ] 卡点思考题有没有直接问 AI 答案？
- [ ] 口述自检是不是闭卷完成的？
- [ ] 本周新学的概念，我能不看资料讲给别人听吗？
- [ ] 如果断网一天，我本周学的东西我能独立动手吗？

**最后一题是关键**。如果答案是"不行"，你的学习方式要调整。

---

## 红线（绝不能越）

1. **M2 Week 5-6 的手写 SSE parser 和 ReAct loop**：**零 AI 代码补全**。这两周的代码必须是你自己敲的每一个字符。**这是 6 个月最重要的底线。**

2. **所有"今天结束能回答" / 周复盘口述**：闭卷完成，不允许查资料查 AI。

3. **Layer 4 清单里的 10 项能力**：每月至少各手写一次验证还在肌肉里。

4. **卡点思考题**：自己想。想不出来先翻 docs，docs 找不到再问 AI —— 但问法必须是"验证型"或"挑战型"，不是"给我答案"。

---

## 最后

这份文档不是给你看一次就完了。

**建议**：
- 贴到显示器边上
- 每周复盘时对照自测
- 每次打开 Claude 之前先问一句："我现在要做的事是 Layer 几？"

6 个月后，当别人用 AI 做出"速成 Agent 工程师"的幻觉，而你做到了**真正的内化**—— 差距就在每天这些看起来"多花的 20 分钟"里。
